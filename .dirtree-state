# Dirtree: Stateful directory trees in the CLI for humans and LLMs. https://github.com/pmarreck/dirtree
ver=1.2
annotate=[
	bin/post = LuaJIT human/agent mail CLI entry point
	docs/ARCHITECTURE_AND_OPERATIONS.md = Public architecture, setup, usage, security, and operations guide
	docs/SMIME.md = Offline CA ceremony, Apple Mail identity import, live verification gate, renewal, and YubiKey migration guide
	docs/TMUX_WAKE.md = Measured explanation of why Codex mail wakeups require a focused PTY terminal client
	flake.nix = Pinned LuaJIT/Himalaya development environment and future package/module outputs
	nix/module.nix = Reusable NixOS module for tailnet-only Postfix, Dovecot, credentials, TLS, and Maildir
	nix/package.nix = Nix derivation for the packaged post CLI and its LuaJIT runtime
	src/app.lua = Pure command planner and project/mailbox context resolver for the post CLI
	src/args.lua = Pure order-independent parser for the post CLI
	src/identity.lua = Pure project identity validation, collision classification, and mail address/folder derivation
	src/maildir.lua = Discovers unread project deliveries from server-local Maildir new directories
	src/process.lua = Shell-free luv process adapter for invoking the Himalaya transport
	src/review.lua = Pure RFC-style candidate renderer and conservative confirmation classifier
	src/secure_files.lua = Bounded reads and collision-safe protected output writes for key ceremonies
	src/smime_ceremony.lua = Pure orchestration for offline root creation and Apple S/MIME identity issuance
	src/smime_replay.lua = Exclusive-create signature claims that reject concurrent or later S/MIME replays
	src/smime_verification.lua = Pure boundary that verifies signed content before durably claiming its replay key
	src/tmux_wake.lua = Classifies exact tmux cursor prompts and constructs fixed notice/wake commands
	src/transport.lua = Shell-free Himalaya argv construction
	src/wake.lua = Pure conservative agent wake-decision state machine
	src/watch.lua = Pure persistent delivery notice and wake state transition logic
	src/watch_runtime.lua = Connects Maildir scanning, tmux probing, wake policy, actions, and state persistence
	src/watch_state.lua = Validates and atomically persists watcher JSON state
	tests = Unit and CLI contract tests
	tests/fixtures/fake_himalaya = Deterministic Himalaya stand-in used by CLI integration tests
	tests/fixtures/fake_tmux = Deterministic tmux adapter fixture for watcher CLI tests
	tests/fixtures/process_echo = Deterministic stdin echo fixture for process adapter tests
	tests/fixtures/process_large_output = Pipe-buffer-sized output fixture guarding against capture truncation
	tests/integration/maildir_spec.lua = Tests set-based project Maildir delivery discovery
	tests/integration/process_spec.lua = Integration coverage for stdin streaming and complete asynchronous pipe drainage
	tests/integration/secure_files_spec.lua = Real filesystem mode, atomic publication, collision, and read-bound tests
	tests/integration/smime_replay_spec.lua = Durable replay claim, permissions, and malformed-key tests
	tests/integration/watch_state_spec.lua = Tests missing, round-trip, permissions, and corrupt watcher state
	tests/nix/module_eval.nix = Pure NixOS module policy and wiring assertions
	tests/nix/server_integration.nix = Isolated NixOS VM proof of authenticated SMTP-to-LMTP-to-Maildir-to-IMAPS delivery and relay rejection
	tests/unit/app_spec.lua = Unit coverage for post command planning and repository context inference
	tests/unit/review_spec.lua = Exact message preview and set-classified confirmation behavior
	tests/unit/smime_ceremony_spec.lua = Pure ceremony orchestration and secret-source behavior tests
	tests/unit/smime_verification_spec.lua = Verification ordering test proving claim precedes authenticated-content return
	tests/unit/tmux_wake_spec.lua = Tests pane selection, cursor-row prompt probes, ambiguity, and tmux argv
	tests/unit/watch_runtime_spec.lua = Tests action ordering, failure persistence boundary, and authorization parsing
	tests/unit/watch_spec.lua = Tests delivery notification, deferred wake, cooldown, deduplication, and pruning
]
